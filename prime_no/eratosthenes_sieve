#include<bits/stdc++.h>
using namespace std;

vector<bool> prime(int n){
    vector<bool> prime(n+1,true);
    prime[0] = prime[1] = false;
    for(int i=2;i*i<=n;i++){
        if(prime[i]==true){
            // j = i*2;
            int j = i*i;

            while(j<=n){
                prime[j] = false;
                j+=i;
            }
        }
    }
    return prime;
}

int main(){
    int n ;
    cin >>n;
    vector<bool> sieve = prime(n);
    for(int i=0;i<n;i++){
        if(sieve[i]){
            cout<<i<<" ";

        }
    }
    return 0;
}